# 2026-05-16

## A股恐慌看板 - 删除日跌幅图表，增加历史信号记录列表

- 删除 panic_dashboard.html 中的沪深300日跌幅历史图表（Chart.js 及 annotation 插件引用）
- 新增历史信号记录卡片列表UI，参考用户截图设计：
  - 每个信号卡片包含：日期、恐慌级别标签、跌幅/量比/下跌比/PE、5日/20日/60日后续收益
  - 收益颜色遵循A股惯例：正收益红、负收益绿；跌幅用绿色
  - 级别标签：一级(黄)、二级(橙)、三级(红)
  - 数据不足显示 "--"
- 修改 panic_monitor.py：
  - `append_history` 增加 `signal_close`、`return_5d`、`return_20d`、`return_60d` 字段
  - 新增 `update_signal_returns(history)` 函数：批量获取信号日到当前的日线数据，计算后续第5/20/60个交易日收益
  - 删除 `build_history_chart_data` 函数及 `history_cache.json` 缓存逻辑
  - `update_dashboard_html` 的 SNAP 数据精简，仅保留 `alertHistory`（含收益字段）
  - `main` 函数在多处调用 `update_signal_returns` 确保历史信号收益及时更新
- 脚本运行测试通过（2026-05-15 未触发，正常更新看板）
- 预览 panic_dashboard.html 效果正常

## 文件变更
- `E:\WorkBuddy\A股恐慌看板-3\panic_dashboard.html` - 重写（删除图表，新增信号列表）
- `E:\WorkBuddy\A股恐慌看板-3\panic_monitor.py` - 修改（新增收益计算，删除图表数据构建）
